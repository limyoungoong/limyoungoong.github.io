# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

# LLM guides / instructions (keep private even in public repo)
# Note: CLAUDE.md is NOT ignored — it's a minimal bootstrap (tech orientation + memory setup)
#       committed on purpose so Claude on a fresh PC knows to ask for the Dropbox memory path.
#       Sensitive editorial/branding content lives in the external memory folder, never in the repo.
CLAUDE.local.md
AGENTS.md
GEMINI.md
.claude/
.cursor/
.cursorrules
.aider*
.windsurfrules
